<Page x:Class="FlightTicketManager.GUI.XuLyYeuCauStaffPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Xử lý yêu cầu">

    <Grid Background="#F8FAFD">
        <Border Background="White" Margin="20" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="GHI NHẬN YÊU CẦU KHÁCH HÀNG"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#1E88E5"
                           Margin="0 0 0 20"/>

                <Border Grid.Row="1" Background="#F5F9FF" CornerRadius="10" Padding="15" Margin="0 0 0 20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="180"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã vé:" Margin="0 0 10 12" VerticalAlignment="Center"/>
                        <TextBox x:Name="TxtMaVeYeuCau" Grid.Row="0" Grid.Column="1" Margin="0 0 15 12"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Loại yêu cầu:" Margin="0 0 10 12" VerticalAlignment="Center"/>
                        <ComboBox x:Name="CboLoaiYeuCau" Grid.Row="0" Grid.Column="3" Margin="0 0 0 12" SelectedIndex="0">
                            <ComboBoxItem Content="Hủy vé"/>
                            <ComboBoxItem Content="Cập nhật thông tin"/>
                            <ComboBoxItem Content="Đổi chuyến bay"/>
                            <ComboBoxItem Content="Khác"/>
                        </ComboBox>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Lý do:" Margin="0 0 10 12" VerticalAlignment="Top"/>
                        <TextBox x:Name="TxtLyDoYeuCau"
                                 Grid.Row="1"
                                 Grid.Column="1"
                                 Grid.ColumnSpan="3"
                                 Height="80"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0 0 0 12"/>

                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal">
                            <Button Content="Ghi nhận yêu cầu"
                                    Width="160"
                                    Height="38"
                                    Background="#43A047"
                                    Foreground="White"
                                    Margin="0 0 10 0"
                                    Click="BtnGhiNhanYeuCau_Click"/>

                            <Button Content="Tải dữ liệu demo"
                                    Width="150"
                                    Height="38"
                                    Background="#1E88E5"
                                    Foreground="White"
                                    Click="BtnTaiDuLieuDemo_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid x:Name="DgYeuCau"
                          Grid.Row="2"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã yêu cầu" Binding="{Binding MaYeuCau}" Width="100"/>
                        <DataGridTextColumn Header="Mã vé" Binding="{Binding MaVe}" Width="100"/>
                        <DataGridTextColumn Header="Loại yêu cầu" Binding="{Binding LoaiYeuCau}" Width="150"/>
                        <DataGridTextColumn Header="Lý do" Binding="{Binding LyDo}" Width="*"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
